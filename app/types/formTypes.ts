export type formEventType = {
  target: {
    name: string;
    value: string;
  };
};

export type contactInfoType = {
  name: string;
  email: string;
  message: string;
};

export type contactInfoPropType = {
  contactInfo: {
    name: string;
    email: string;
    message: string;
  };
};
